import Grid from "@/components/Grid/Grid";
import React from "react";

const About = () => {
  return (
    <section id="about">
      <Grid />
    </section>
  );
};

export default About;
